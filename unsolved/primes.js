// Advanced:
// 1. Write a method to generate/print/store the first "n" prime numbers.
// Hint: 1 is not a prime number. The first prime number is 2.

function primes(n) {
	if (n < 2){
		console.log(0);
		return 0;
	}
	var arr = [2];

	var i = 0;
	var x = 0;

	while(arr.length < n){
		// var prime = false;
		if (n % arr[i] == 0){

		}
		i += 1;
	}


	
}
